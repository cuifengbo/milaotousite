
export type User = {
    id: string;
    name: string;
    email: string;
    password: string;
};

export type Blog = {
    id: string;
    title: string;
    content: string;
    image_url: string;
};

export type Product = {
    id: string;
    title: string;
    content: string;
    image_url: string;
};

export type UsefulLink = {
    id: string;
    title: string;
    content: string;
    image_url: string;
};